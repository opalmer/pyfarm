#!/usr/bin/python

from lib.sequence import Sequence
from lib.sequence import Info

#s = Sequence('/home/drive', 'thisFrame', 3, 1, 50, 1, 'jpg')
#seq = list(s.seq())
#print seq


i = Info('Frames')
i.get()
