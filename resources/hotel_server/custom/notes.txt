Server Actions:
	# job = JobID
	# frame = frame in job
	# obj = object to query
	#	+ Exit Status
	#	+ Current status
	#	+ Progress
	render(job, frame)
	kill_render(job, frame)
	query(job, frame, obj)
	monitor_log(job, frame)
	
Client Actions:
		
