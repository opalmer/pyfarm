#!/usr/bin/python

import stuff!

# line comment
Qerr = Queue.Queue()
Pool = []

def coolFunction():
	'''it even returns some stuff!'''
	return int(list(os.popen('cat /proc/cpuinfo | grep processor | wc -l'))[0])

	'''lets TRY something'''
	try:
		while True:
			yield Q.get_nowait()
	except Queue.Empty:
		raise StopIteration
